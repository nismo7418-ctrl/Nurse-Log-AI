# NurseLog AI - Requirements
# Prototype MVP — Documentation Infirmière IA Belgique

# Interface utilisateur
streamlit>=1.30.0

# Pour exports PDF (production)
reportlab>=4.0.0

# Pour intégration eHealth (production)
requests>=2.31.0
pydantic>=2.5.0

# Pour reconnaissance vocale (production)
# Option A : API OpenAI Whisper (nécessite OPENAI_API_KEY, audio envoyé à OpenAI)
openai>=1.10.0
# Option B : Whisper local 100% RGPD (aucune clé, aucune donnée envoyée)
# openai-whisper>=20231117
#   + ffmpeg requis par Whisper pour décoder l'audio (non distribué via pip) :
#     Windows : winget install Gyan.FFmpeg  (ou choco install ffmpeg)
#     macOS   : brew install ffmpeg
#     Linux   : sudo apt install ffmpeg
#   NB : le moteur détecte automatiquement un ffmpeg installé hors du PATH
#        (winget, scoop, Homebrew) — voir nurselog_engine._chercher_ffmpeg()

# Pour le relay audio local (micro navigateur → transcription)
# → audio_relay.py utilise uniquement la stdlib Python (http.server, threading)
# → aucune dépendance supplémentaire requise

# Développement & tests
pytest>=7.4.0

# Pour CSV export
pandas>=2.0.0
